// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.0
//   protoc               v7.35.1
// source: quip/v1/miner.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientDuplexStream,
  type ClientOptions,
  type handleBidiStreamingCall,
  makeGenericClientConstructor,
  type Metadata,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "quip.v1";

export enum JobKind {
  JOB_KIND_UNSPECIFIED = 0,
  ISING_SAMPLE = 1,
  /** GATE_CIRCUIT - reserved */
  GATE_CIRCUIT = 2,
  UNRECOGNIZED = -1,
}

export function jobKindFromJSON(object: any): JobKind {
  switch (object) {
    case 0:
    case "JOB_KIND_UNSPECIFIED":
      return JobKind.JOB_KIND_UNSPECIFIED;
    case 1:
    case "ISING_SAMPLE":
      return JobKind.ISING_SAMPLE;
    case 2:
    case "GATE_CIRCUIT":
      return JobKind.GATE_CIRCUIT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return JobKind.UNRECOGNIZED;
  }
}

export function jobKindToJSON(object: JobKind): string {
  switch (object) {
    case JobKind.JOB_KIND_UNSPECIFIED:
      return "JOB_KIND_UNSPECIFIED";
    case JobKind.ISING_SAMPLE:
      return "ISING_SAMPLE";
    case JobKind.GATE_CIRCUIT:
      return "GATE_CIRCUIT";
    case JobKind.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum RejectReason {
  REJECT_REASON_UNSPECIFIED = 0,
  UNSUPPORTED_KIND = 1,
  TOO_LARGE = 2,
  EXPIRED = 3,
  OVERLOADED = 4,
  SHUTTING_DOWN = 5,
  MALFORMED = 6,
  TOPOLOGY_MISMATCH = 7,
  TOPOLOGY_MISSING = 8,
  UNRECOGNIZED = -1,
}

export function rejectReasonFromJSON(object: any): RejectReason {
  switch (object) {
    case 0:
    case "REJECT_REASON_UNSPECIFIED":
      return RejectReason.REJECT_REASON_UNSPECIFIED;
    case 1:
    case "UNSUPPORTED_KIND":
      return RejectReason.UNSUPPORTED_KIND;
    case 2:
    case "TOO_LARGE":
      return RejectReason.TOO_LARGE;
    case 3:
    case "EXPIRED":
      return RejectReason.EXPIRED;
    case 4:
    case "OVERLOADED":
      return RejectReason.OVERLOADED;
    case 5:
    case "SHUTTING_DOWN":
      return RejectReason.SHUTTING_DOWN;
    case 6:
    case "MALFORMED":
      return RejectReason.MALFORMED;
    case 7:
    case "TOPOLOGY_MISMATCH":
      return RejectReason.TOPOLOGY_MISMATCH;
    case 8:
    case "TOPOLOGY_MISSING":
      return RejectReason.TOPOLOGY_MISSING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return RejectReason.UNRECOGNIZED;
  }
}

export function rejectReasonToJSON(object: RejectReason): string {
  switch (object) {
    case RejectReason.REJECT_REASON_UNSPECIFIED:
      return "REJECT_REASON_UNSPECIFIED";
    case RejectReason.UNSUPPORTED_KIND:
      return "UNSUPPORTED_KIND";
    case RejectReason.TOO_LARGE:
      return "TOO_LARGE";
    case RejectReason.EXPIRED:
      return "EXPIRED";
    case RejectReason.OVERLOADED:
      return "OVERLOADED";
    case RejectReason.SHUTTING_DOWN:
      return "SHUTTING_DOWN";
    case RejectReason.MALFORMED:
      return "MALFORMED";
    case RejectReason.TOPOLOGY_MISMATCH:
      return "TOPOLOGY_MISMATCH";
    case RejectReason.TOPOLOGY_MISSING:
      return "TOPOLOGY_MISSING";
    case RejectReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface MinerMsg {
  hello?: Hello | undefined;
  ready?: Ready | undefined;
  jobRequest?: JobRequest | undefined;
  result?: Result | undefined;
  reject?: Reject | undefined;
  status?: Status | undefined;
  fatal?: Fatal | undefined;
  capabilities?: Capabilities | undefined;
}

export interface CoordMsg {
  welcome?: Welcome | undefined;
  configure?: Configure | undefined;
  topology?: Topology | undefined;
  job?: Job | undefined;
  cancel?: Cancel | undefined;
  ping?: Ping | undefined;
  shutdown?: Shutdown | undefined;
  setTarget?: SetTarget | undefined;
  getCapabilities?: GetCapabilities | undefined;
}

export interface Hello {
  minerId: string;
  sessionToken: string;
  /** this spec: 1 */
  protocolVersion: number;
  /** cpu | cuda | metal | dwave-qpu */
  backend: string;
  /** sa | gibbs | quantum-anneal */
  algorithm: string;
  supportedKinds: JobKind[];
  maxNodes: number;
  maxEdges: number;
  nativeTopologyHash?: Uint8Array | undefined;
  features: string[];
}

export interface Welcome {
  protocolVersion: number;
}

export interface Configure {
  queueDepth: number;
  idleTimeoutS: number;
  heartbeatS: number;
  reconnectWindowS: number;
  /** opaque per-backend config, passed through verbatim */
  backendToml: string;
}

export interface Ready {
}

export interface Topology {
  hash: Uint8Array;
  nodes: number[];
  edges:
    | EdgeList
    | undefined;
  /** for the miner's adapt difficulty band */
  allowedHMilli: number[];
}

/**
 * Difficulty target, pushed by the coordinator after Topology and re-sent
 * whenever difficulty changes. The miner adapts its sampling budget from
 * max_energy_milli; num_reads/num_sweeps/anneal_time_us are optional explicit
 * overrides (0 = unset -> adapt).
 */
export interface SetTarget {
  /** acceptance ceiling AND adapt target */
  maxEnergyMilli: bigint;
  minSolutions: number;
  minDiversityMilli: number;
  /** 0 = unset */
  numReads: number;
  /** 0 = unset */
  numSweeps: number;
  /** 0 = unset */
  annealTimeUs: number;
}

export interface EdgeList {
  u: number[];
  v: number[];
}

export interface IsingProblem {
  topologyHash?: Uint8Array | undefined;
  edges?: EdgeList | undefined;
  hMilliLe32: Uint8Array;
  /**
   * Coupling coefficients, little-endian int32 milli-units. Normative
   * invariant: len(j_milli_le32) == len(edges) after decoding (one i32 per
   * edge, in the same order as EdgeList.u/v or the cached Topology edges).
   */
  jMilliLe32: Uint8Array;
  /** per-job override; 0 = unset -> SetTarget/adapt */
  numReads: number;
  /** per-job override; 0 = unset */
  numSweeps: number;
  /** per-job override; 0 = unset */
  annealTimeUs: number;
  /**
   * Optional warm-start states, one entry per seeded read, best state first.
   * Each entry is bit-packed in topology node order: node i is bit (i % 8) of
   * byte (i / 8), LSB first; bit 1 = spin +1, bit 0 = spin -1. An entry is
   * exactly ceil(num_nodes / 8) bytes and its padding bits are 0. Empty =
   * unset: the solver picks its own start. A solver that does not advertise
   * the `initial-spins` feature ignores this field and fields 10 to 12.
   */
  initialSpins: Uint8Array[];
  /**
   * Seeded SA: inverse temperature the anneal starts from, milli-units.
   * 0 = the solver picks. Ignored when initial_spins is empty.
   */
  startBetaMilli: number;
  /**
   * Seeded QPU: anneal fraction s the reverse anneal backs off to, in
   * milli-units (1..999). 0 = the solver picks. Ignored when initial_spins is
   * empty.
   */
  reversalSMilli: number;
  /**
   * Seeded QPU: pause at the reversal point, microseconds. 0 = the solver
   * picks. Ignored when initial_spins is empty.
   */
  reversalPauseUs: number;
}

export interface Provenance {
  isPow: boolean;
  /** mempool reward attribution; empty for PoW */
  orderId: Uint8Array;
}

export interface Job {
  jobId: Uint8Array;
  kind: JobKind;
  /** PoW cancellation scope; 0 for mempool */
  generation: bigint;
  deadlineMs: bigint;
  ising: IsingProblem | undefined;
  provenance: Provenance | undefined;
}

export interface JobRequest {
  credits: number;
}

export interface Cancel {
  maxGeneration: bigint;
}

export interface Ping {
}

export interface Shutdown {
  graceMs: number;
}

export interface Solution {
  /** one byte/spin: 0x01=+1, 0xFF=-1 */
  spinsBytes: Uint8Array;
  energyMilli: bigint;
}

export interface SamplerMeta {
  reads: number;
  sweeps: number;
  deviceAccessTimeUs: bigint;
  qpuAccessUs: bigint;
  extra: { [key: string]: string };
}

export interface SamplerMeta_ExtraEntry {
  key: string;
  value: string;
}

export interface Result {
  jobId: Uint8Array;
  solutions: Solution[];
  meta: SamplerMeta | undefined;
}

export interface Reject {
  jobId: Uint8Array;
  reason: RejectReason;
}

export interface Status {
  minerId: string;
  utilization: number;
  jobsDone: bigint;
  /** cancelled in-flight work reported here, not in Result */
  abandonedGeneration: bigint;
  samplerStats: { [key: string]: string };
}

export interface Status_SamplerStatsEntry {
  key: string;
  value: string;
}

export interface Fatal {
  /** 64/69/70/77 */
  exitCode: number;
  reason: string;
  restartRequired: boolean;
}

/**
 * Coordinator asks a miner what it currently supports. A miner may also send
 * `Capabilities` unprompted when what it supports changes mid-session, for
 * example a degraded device lowering `max_nodes`.
 */
export interface GetCapabilities {
}

/**
 * What a solver supports on this host. A caller uses it to decide whether to
 * launch the binary at all, so it must answer without touching the device.
 */
export interface Capabilities {
  /** cpu | cuda | metal | dwave-qpu */
  backend: string;
  /** sa | gibbs | quantum-anneal */
  algorithm: string;
  supportedKinds: JobKind[];
  maxNodes: number;
  maxEdges: number;
  features: string[];
  /** this spec: 1 */
  protocolVersion: number;
  /** models kept in flight; 1 = serial */
  streamWidth: number;
  nativeTopologyHash?: Uint8Array | undefined;
}

function createBaseMinerMsg(): MinerMsg {
  return {
    hello: undefined,
    ready: undefined,
    jobRequest: undefined,
    result: undefined,
    reject: undefined,
    status: undefined,
    fatal: undefined,
    capabilities: undefined,
  };
}

export const MinerMsg: MessageFns<MinerMsg> = {
  encode(message: MinerMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hello !== undefined) {
      Hello.encode(message.hello, writer.uint32(10).fork()).join();
    }
    if (message.ready !== undefined) {
      Ready.encode(message.ready, writer.uint32(18).fork()).join();
    }
    if (message.jobRequest !== undefined) {
      JobRequest.encode(message.jobRequest, writer.uint32(26).fork()).join();
    }
    if (message.result !== undefined) {
      Result.encode(message.result, writer.uint32(34).fork()).join();
    }
    if (message.reject !== undefined) {
      Reject.encode(message.reject, writer.uint32(42).fork()).join();
    }
    if (message.status !== undefined) {
      Status.encode(message.status, writer.uint32(50).fork()).join();
    }
    if (message.fatal !== undefined) {
      Fatal.encode(message.fatal, writer.uint32(58).fork()).join();
    }
    if (message.capabilities !== undefined) {
      Capabilities.encode(message.capabilities, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MinerMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMinerMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hello = Hello.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.ready = Ready.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.jobRequest = JobRequest.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.result = Result.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.reject = Reject.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.status = Status.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.fatal = Fatal.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.capabilities = Capabilities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MinerMsg {
    return {
      hello: isSet(object.hello) ? Hello.fromJSON(object.hello) : undefined,
      ready: isSet(object.ready) ? Ready.fromJSON(object.ready) : undefined,
      jobRequest: isSet(object.jobRequest)
        ? JobRequest.fromJSON(object.jobRequest)
        : isSet(object.job_request)
        ? JobRequest.fromJSON(object.job_request)
        : undefined,
      result: isSet(object.result) ? Result.fromJSON(object.result) : undefined,
      reject: isSet(object.reject) ? Reject.fromJSON(object.reject) : undefined,
      status: isSet(object.status) ? Status.fromJSON(object.status) : undefined,
      fatal: isSet(object.fatal) ? Fatal.fromJSON(object.fatal) : undefined,
      capabilities: isSet(object.capabilities) ? Capabilities.fromJSON(object.capabilities) : undefined,
    };
  },

  toJSON(message: MinerMsg): unknown {
    const obj: any = {};
    if (message.hello !== undefined) {
      obj.hello = Hello.toJSON(message.hello);
    }
    if (message.ready !== undefined) {
      obj.ready = Ready.toJSON(message.ready);
    }
    if (message.jobRequest !== undefined) {
      obj.jobRequest = JobRequest.toJSON(message.jobRequest);
    }
    if (message.result !== undefined) {
      obj.result = Result.toJSON(message.result);
    }
    if (message.reject !== undefined) {
      obj.reject = Reject.toJSON(message.reject);
    }
    if (message.status !== undefined) {
      obj.status = Status.toJSON(message.status);
    }
    if (message.fatal !== undefined) {
      obj.fatal = Fatal.toJSON(message.fatal);
    }
    if (message.capabilities !== undefined) {
      obj.capabilities = Capabilities.toJSON(message.capabilities);
    }
    return obj;
  },

  create(base?: DeepPartial<MinerMsg>): MinerMsg {
    return MinerMsg.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MinerMsg>): MinerMsg {
    const message = createBaseMinerMsg();
    message.hello = (object.hello !== undefined && object.hello !== null) ? Hello.fromPartial(object.hello) : undefined;
    message.ready = (object.ready !== undefined && object.ready !== null) ? Ready.fromPartial(object.ready) : undefined;
    message.jobRequest = (object.jobRequest !== undefined && object.jobRequest !== null)
      ? JobRequest.fromPartial(object.jobRequest)
      : undefined;
    message.result = (object.result !== undefined && object.result !== null)
      ? Result.fromPartial(object.result)
      : undefined;
    message.reject = (object.reject !== undefined && object.reject !== null)
      ? Reject.fromPartial(object.reject)
      : undefined;
    message.status = (object.status !== undefined && object.status !== null)
      ? Status.fromPartial(object.status)
      : undefined;
    message.fatal = (object.fatal !== undefined && object.fatal !== null) ? Fatal.fromPartial(object.fatal) : undefined;
    message.capabilities = (object.capabilities !== undefined && object.capabilities !== null)
      ? Capabilities.fromPartial(object.capabilities)
      : undefined;
    return message;
  },
};

function createBaseCoordMsg(): CoordMsg {
  return {
    welcome: undefined,
    configure: undefined,
    topology: undefined,
    job: undefined,
    cancel: undefined,
    ping: undefined,
    shutdown: undefined,
    setTarget: undefined,
    getCapabilities: undefined,
  };
}

export const CoordMsg: MessageFns<CoordMsg> = {
  encode(message: CoordMsg, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.welcome !== undefined) {
      Welcome.encode(message.welcome, writer.uint32(10).fork()).join();
    }
    if (message.configure !== undefined) {
      Configure.encode(message.configure, writer.uint32(18).fork()).join();
    }
    if (message.topology !== undefined) {
      Topology.encode(message.topology, writer.uint32(26).fork()).join();
    }
    if (message.job !== undefined) {
      Job.encode(message.job, writer.uint32(34).fork()).join();
    }
    if (message.cancel !== undefined) {
      Cancel.encode(message.cancel, writer.uint32(42).fork()).join();
    }
    if (message.ping !== undefined) {
      Ping.encode(message.ping, writer.uint32(50).fork()).join();
    }
    if (message.shutdown !== undefined) {
      Shutdown.encode(message.shutdown, writer.uint32(58).fork()).join();
    }
    if (message.setTarget !== undefined) {
      SetTarget.encode(message.setTarget, writer.uint32(66).fork()).join();
    }
    if (message.getCapabilities !== undefined) {
      GetCapabilities.encode(message.getCapabilities, writer.uint32(74).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CoordMsg {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCoordMsg();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.welcome = Welcome.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.configure = Configure.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.topology = Topology.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.job = Job.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.cancel = Cancel.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.ping = Ping.decode(reader, reader.uint32());
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.shutdown = Shutdown.decode(reader, reader.uint32());
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.setTarget = SetTarget.decode(reader, reader.uint32());
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.getCapabilities = GetCapabilities.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CoordMsg {
    return {
      welcome: isSet(object.welcome) ? Welcome.fromJSON(object.welcome) : undefined,
      configure: isSet(object.configure) ? Configure.fromJSON(object.configure) : undefined,
      topology: isSet(object.topology) ? Topology.fromJSON(object.topology) : undefined,
      job: isSet(object.job) ? Job.fromJSON(object.job) : undefined,
      cancel: isSet(object.cancel) ? Cancel.fromJSON(object.cancel) : undefined,
      ping: isSet(object.ping) ? Ping.fromJSON(object.ping) : undefined,
      shutdown: isSet(object.shutdown) ? Shutdown.fromJSON(object.shutdown) : undefined,
      setTarget: isSet(object.setTarget)
        ? SetTarget.fromJSON(object.setTarget)
        : isSet(object.set_target)
        ? SetTarget.fromJSON(object.set_target)
        : undefined,
      getCapabilities: isSet(object.getCapabilities)
        ? GetCapabilities.fromJSON(object.getCapabilities)
        : isSet(object.get_capabilities)
        ? GetCapabilities.fromJSON(object.get_capabilities)
        : undefined,
    };
  },

  toJSON(message: CoordMsg): unknown {
    const obj: any = {};
    if (message.welcome !== undefined) {
      obj.welcome = Welcome.toJSON(message.welcome);
    }
    if (message.configure !== undefined) {
      obj.configure = Configure.toJSON(message.configure);
    }
    if (message.topology !== undefined) {
      obj.topology = Topology.toJSON(message.topology);
    }
    if (message.job !== undefined) {
      obj.job = Job.toJSON(message.job);
    }
    if (message.cancel !== undefined) {
      obj.cancel = Cancel.toJSON(message.cancel);
    }
    if (message.ping !== undefined) {
      obj.ping = Ping.toJSON(message.ping);
    }
    if (message.shutdown !== undefined) {
      obj.shutdown = Shutdown.toJSON(message.shutdown);
    }
    if (message.setTarget !== undefined) {
      obj.setTarget = SetTarget.toJSON(message.setTarget);
    }
    if (message.getCapabilities !== undefined) {
      obj.getCapabilities = GetCapabilities.toJSON(message.getCapabilities);
    }
    return obj;
  },

  create(base?: DeepPartial<CoordMsg>): CoordMsg {
    return CoordMsg.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CoordMsg>): CoordMsg {
    const message = createBaseCoordMsg();
    message.welcome = (object.welcome !== undefined && object.welcome !== null)
      ? Welcome.fromPartial(object.welcome)
      : undefined;
    message.configure = (object.configure !== undefined && object.configure !== null)
      ? Configure.fromPartial(object.configure)
      : undefined;
    message.topology = (object.topology !== undefined && object.topology !== null)
      ? Topology.fromPartial(object.topology)
      : undefined;
    message.job = (object.job !== undefined && object.job !== null) ? Job.fromPartial(object.job) : undefined;
    message.cancel = (object.cancel !== undefined && object.cancel !== null)
      ? Cancel.fromPartial(object.cancel)
      : undefined;
    message.ping = (object.ping !== undefined && object.ping !== null) ? Ping.fromPartial(object.ping) : undefined;
    message.shutdown = (object.shutdown !== undefined && object.shutdown !== null)
      ? Shutdown.fromPartial(object.shutdown)
      : undefined;
    message.setTarget = (object.setTarget !== undefined && object.setTarget !== null)
      ? SetTarget.fromPartial(object.setTarget)
      : undefined;
    message.getCapabilities = (object.getCapabilities !== undefined && object.getCapabilities !== null)
      ? GetCapabilities.fromPartial(object.getCapabilities)
      : undefined;
    return message;
  },
};

function createBaseHello(): Hello {
  return {
    minerId: "",
    sessionToken: "",
    protocolVersion: 0,
    backend: "",
    algorithm: "",
    supportedKinds: [],
    maxNodes: 0,
    maxEdges: 0,
    nativeTopologyHash: undefined,
    features: [],
  };
}

export const Hello: MessageFns<Hello> = {
  encode(message: Hello, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.minerId !== "") {
      writer.uint32(10).string(message.minerId);
    }
    if (message.sessionToken !== "") {
      writer.uint32(18).string(message.sessionToken);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(24).uint32(message.protocolVersion);
    }
    if (message.backend !== "") {
      writer.uint32(34).string(message.backend);
    }
    if (message.algorithm !== "") {
      writer.uint32(42).string(message.algorithm);
    }
    writer.uint32(50).fork();
    for (const v of message.supportedKinds) {
      writer.int32(v);
    }
    writer.join();
    if (message.maxNodes !== 0) {
      writer.uint32(56).uint32(message.maxNodes);
    }
    if (message.maxEdges !== 0) {
      writer.uint32(64).uint32(message.maxEdges);
    }
    if (message.nativeTopologyHash !== undefined) {
      writer.uint32(74).bytes(message.nativeTopologyHash);
    }
    for (const v of message.features) {
      writer.uint32(82).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hello {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHello();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.minerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionToken = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.backend = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.supportedKinds.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedKinds.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.maxNodes = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.maxEdges = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.nativeTopologyHash = reader.bytes();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.features.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hello {
    return {
      minerId: isSet(object.minerId)
        ? globalThis.String(object.minerId)
        : isSet(object.miner_id)
        ? globalThis.String(object.miner_id)
        : "",
      sessionToken: isSet(object.sessionToken)
        ? globalThis.String(object.sessionToken)
        : isSet(object.session_token)
        ? globalThis.String(object.session_token)
        : "",
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      backend: isSet(object.backend) ? globalThis.String(object.backend) : "",
      algorithm: isSet(object.algorithm) ? globalThis.String(object.algorithm) : "",
      supportedKinds: globalThis.Array.isArray(object?.supportedKinds)
        ? object.supportedKinds.map((e: any) => jobKindFromJSON(e))
        : globalThis.Array.isArray(object?.supported_kinds)
        ? object.supported_kinds.map((e: any) => jobKindFromJSON(e))
        : [],
      maxNodes: isSet(object.maxNodes)
        ? globalThis.Number(object.maxNodes)
        : isSet(object.max_nodes)
        ? globalThis.Number(object.max_nodes)
        : 0,
      maxEdges: isSet(object.maxEdges)
        ? globalThis.Number(object.maxEdges)
        : isSet(object.max_edges)
        ? globalThis.Number(object.max_edges)
        : 0,
      nativeTopologyHash: isSet(object.nativeTopologyHash)
        ? bytesFromBase64(object.nativeTopologyHash)
        : isSet(object.native_topology_hash)
        ? bytesFromBase64(object.native_topology_hash)
        : undefined,
      features: globalThis.Array.isArray(object?.features)
        ? object.features.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: Hello): unknown {
    const obj: any = {};
    if (message.minerId !== "") {
      obj.minerId = message.minerId;
    }
    if (message.sessionToken !== "") {
      obj.sessionToken = message.sessionToken;
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.backend !== "") {
      obj.backend = message.backend;
    }
    if (message.algorithm !== "") {
      obj.algorithm = message.algorithm;
    }
    if (message.supportedKinds?.length) {
      obj.supportedKinds = message.supportedKinds.map((e) => jobKindToJSON(e));
    }
    if (message.maxNodes !== 0) {
      obj.maxNodes = Math.round(message.maxNodes);
    }
    if (message.maxEdges !== 0) {
      obj.maxEdges = Math.round(message.maxEdges);
    }
    if (message.nativeTopologyHash !== undefined) {
      obj.nativeTopologyHash = base64FromBytes(message.nativeTopologyHash);
    }
    if (message.features?.length) {
      obj.features = message.features;
    }
    return obj;
  },

  create(base?: DeepPartial<Hello>): Hello {
    return Hello.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Hello>): Hello {
    const message = createBaseHello();
    message.minerId = object.minerId ?? "";
    message.sessionToken = object.sessionToken ?? "";
    message.protocolVersion = object.protocolVersion ?? 0;
    message.backend = object.backend ?? "";
    message.algorithm = object.algorithm ?? "";
    message.supportedKinds = object.supportedKinds?.map((e) => e) || [];
    message.maxNodes = object.maxNodes ?? 0;
    message.maxEdges = object.maxEdges ?? 0;
    message.nativeTopologyHash = object.nativeTopologyHash ?? undefined;
    message.features = object.features?.map((e) => e) || [];
    return message;
  },
};

function createBaseWelcome(): Welcome {
  return { protocolVersion: 0 };
}

export const Welcome: MessageFns<Welcome> = {
  encode(message: Welcome, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.protocolVersion !== 0) {
      writer.uint32(8).uint32(message.protocolVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Welcome {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWelcome();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Welcome {
    return {
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
    };
  },

  toJSON(message: Welcome): unknown {
    const obj: any = {};
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    return obj;
  },

  create(base?: DeepPartial<Welcome>): Welcome {
    return Welcome.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Welcome>): Welcome {
    const message = createBaseWelcome();
    message.protocolVersion = object.protocolVersion ?? 0;
    return message;
  },
};

function createBaseConfigure(): Configure {
  return { queueDepth: 0, idleTimeoutS: 0, heartbeatS: 0, reconnectWindowS: 0, backendToml: "" };
}

export const Configure: MessageFns<Configure> = {
  encode(message: Configure, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.queueDepth !== 0) {
      writer.uint32(8).uint32(message.queueDepth);
    }
    if (message.idleTimeoutS !== 0) {
      writer.uint32(16).uint32(message.idleTimeoutS);
    }
    if (message.heartbeatS !== 0) {
      writer.uint32(24).uint32(message.heartbeatS);
    }
    if (message.reconnectWindowS !== 0) {
      writer.uint32(32).uint32(message.reconnectWindowS);
    }
    if (message.backendToml !== "") {
      writer.uint32(42).string(message.backendToml);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Configure {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfigure();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.queueDepth = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.idleTimeoutS = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.heartbeatS = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.reconnectWindowS = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.backendToml = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Configure {
    return {
      queueDepth: isSet(object.queueDepth)
        ? globalThis.Number(object.queueDepth)
        : isSet(object.queue_depth)
        ? globalThis.Number(object.queue_depth)
        : 0,
      idleTimeoutS: isSet(object.idleTimeoutS)
        ? globalThis.Number(object.idleTimeoutS)
        : isSet(object.idle_timeout_s)
        ? globalThis.Number(object.idle_timeout_s)
        : 0,
      heartbeatS: isSet(object.heartbeatS)
        ? globalThis.Number(object.heartbeatS)
        : isSet(object.heartbeat_s)
        ? globalThis.Number(object.heartbeat_s)
        : 0,
      reconnectWindowS: isSet(object.reconnectWindowS)
        ? globalThis.Number(object.reconnectWindowS)
        : isSet(object.reconnect_window_s)
        ? globalThis.Number(object.reconnect_window_s)
        : 0,
      backendToml: isSet(object.backendToml)
        ? globalThis.String(object.backendToml)
        : isSet(object.backend_toml)
        ? globalThis.String(object.backend_toml)
        : "",
    };
  },

  toJSON(message: Configure): unknown {
    const obj: any = {};
    if (message.queueDepth !== 0) {
      obj.queueDepth = Math.round(message.queueDepth);
    }
    if (message.idleTimeoutS !== 0) {
      obj.idleTimeoutS = Math.round(message.idleTimeoutS);
    }
    if (message.heartbeatS !== 0) {
      obj.heartbeatS = Math.round(message.heartbeatS);
    }
    if (message.reconnectWindowS !== 0) {
      obj.reconnectWindowS = Math.round(message.reconnectWindowS);
    }
    if (message.backendToml !== "") {
      obj.backendToml = message.backendToml;
    }
    return obj;
  },

  create(base?: DeepPartial<Configure>): Configure {
    return Configure.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Configure>): Configure {
    const message = createBaseConfigure();
    message.queueDepth = object.queueDepth ?? 0;
    message.idleTimeoutS = object.idleTimeoutS ?? 0;
    message.heartbeatS = object.heartbeatS ?? 0;
    message.reconnectWindowS = object.reconnectWindowS ?? 0;
    message.backendToml = object.backendToml ?? "";
    return message;
  },
};

function createBaseReady(): Ready {
  return {};
}

export const Ready: MessageFns<Ready> = {
  encode(_: Ready, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ready {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReady();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Ready {
    return {};
  },

  toJSON(_: Ready): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<Ready>): Ready {
    return Ready.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<Ready>): Ready {
    const message = createBaseReady();
    return message;
  },
};

function createBaseTopology(): Topology {
  return { hash: new Uint8Array(0), nodes: [], edges: undefined, allowedHMilli: [] };
}

export const Topology: MessageFns<Topology> = {
  encode(message: Topology, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hash.length !== 0) {
      writer.uint32(10).bytes(message.hash);
    }
    writer.uint32(18).fork();
    for (const v of message.nodes) {
      writer.uint32(v);
    }
    writer.join();
    if (message.edges !== undefined) {
      EdgeList.encode(message.edges, writer.uint32(26).fork()).join();
    }
    writer.uint32(34).fork();
    for (const v of message.allowedHMilli) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Topology {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTopology();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.hash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.nodes.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.nodes.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.edges = EdgeList.decode(reader, reader.uint32());
          continue;
        }
        case 4: {
          if (tag === 32) {
            message.allowedHMilli.push(reader.int32());

            continue;
          }

          if (tag === 34) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.allowedHMilli.push(reader.int32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Topology {
    return {
      hash: isSet(object.hash) ? bytesFromBase64(object.hash) : new Uint8Array(0),
      nodes: globalThis.Array.isArray(object?.nodes) ? object.nodes.map((e: any) => globalThis.Number(e)) : [],
      edges: isSet(object.edges) ? EdgeList.fromJSON(object.edges) : undefined,
      allowedHMilli: globalThis.Array.isArray(object?.allowedHMilli)
        ? object.allowedHMilli.map((e: any) => globalThis.Number(e))
        : globalThis.Array.isArray(object?.allowed_h_milli)
        ? object.allowed_h_milli.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: Topology): unknown {
    const obj: any = {};
    if (message.hash.length !== 0) {
      obj.hash = base64FromBytes(message.hash);
    }
    if (message.nodes?.length) {
      obj.nodes = message.nodes.map((e) => Math.round(e));
    }
    if (message.edges !== undefined) {
      obj.edges = EdgeList.toJSON(message.edges);
    }
    if (message.allowedHMilli?.length) {
      obj.allowedHMilli = message.allowedHMilli.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Topology>): Topology {
    return Topology.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Topology>): Topology {
    const message = createBaseTopology();
    message.hash = object.hash ?? new Uint8Array(0);
    message.nodes = object.nodes?.map((e) => e) || [];
    message.edges = (object.edges !== undefined && object.edges !== null)
      ? EdgeList.fromPartial(object.edges)
      : undefined;
    message.allowedHMilli = object.allowedHMilli?.map((e) => e) || [];
    return message;
  },
};

function createBaseSetTarget(): SetTarget {
  return { maxEnergyMilli: 0n, minSolutions: 0, minDiversityMilli: 0, numReads: 0, numSweeps: 0, annealTimeUs: 0 };
}

export const SetTarget: MessageFns<SetTarget> = {
  encode(message: SetTarget, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxEnergyMilli !== 0n) {
      if (BigInt.asIntN(64, message.maxEnergyMilli) !== message.maxEnergyMilli) {
        throw new globalThis.Error("value provided for field message.maxEnergyMilli of type int64 too large");
      }
      writer.uint32(8).int64(message.maxEnergyMilli);
    }
    if (message.minSolutions !== 0) {
      writer.uint32(16).uint32(message.minSolutions);
    }
    if (message.minDiversityMilli !== 0) {
      writer.uint32(24).uint32(message.minDiversityMilli);
    }
    if (message.numReads !== 0) {
      writer.uint32(32).uint32(message.numReads);
    }
    if (message.numSweeps !== 0) {
      writer.uint32(40).uint32(message.numSweeps);
    }
    if (message.annealTimeUs !== 0) {
      writer.uint32(48).uint32(message.annealTimeUs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetTarget {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetTarget();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxEnergyMilli = reader.int64() as bigint;
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.minSolutions = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minDiversityMilli = reader.uint32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.numReads = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.numSweeps = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.annealTimeUs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetTarget {
    return {
      maxEnergyMilli: isSet(object.maxEnergyMilli)
        ? BigInt(object.maxEnergyMilli)
        : isSet(object.max_energy_milli)
        ? BigInt(object.max_energy_milli)
        : 0n,
      minSolutions: isSet(object.minSolutions)
        ? globalThis.Number(object.minSolutions)
        : isSet(object.min_solutions)
        ? globalThis.Number(object.min_solutions)
        : 0,
      minDiversityMilli: isSet(object.minDiversityMilli)
        ? globalThis.Number(object.minDiversityMilli)
        : isSet(object.min_diversity_milli)
        ? globalThis.Number(object.min_diversity_milli)
        : 0,
      numReads: isSet(object.numReads)
        ? globalThis.Number(object.numReads)
        : isSet(object.num_reads)
        ? globalThis.Number(object.num_reads)
        : 0,
      numSweeps: isSet(object.numSweeps)
        ? globalThis.Number(object.numSweeps)
        : isSet(object.num_sweeps)
        ? globalThis.Number(object.num_sweeps)
        : 0,
      annealTimeUs: isSet(object.annealTimeUs)
        ? globalThis.Number(object.annealTimeUs)
        : isSet(object.anneal_time_us)
        ? globalThis.Number(object.anneal_time_us)
        : 0,
    };
  },

  toJSON(message: SetTarget): unknown {
    const obj: any = {};
    if (message.maxEnergyMilli !== 0n) {
      obj.maxEnergyMilli = message.maxEnergyMilli.toString();
    }
    if (message.minSolutions !== 0) {
      obj.minSolutions = Math.round(message.minSolutions);
    }
    if (message.minDiversityMilli !== 0) {
      obj.minDiversityMilli = Math.round(message.minDiversityMilli);
    }
    if (message.numReads !== 0) {
      obj.numReads = Math.round(message.numReads);
    }
    if (message.numSweeps !== 0) {
      obj.numSweeps = Math.round(message.numSweeps);
    }
    if (message.annealTimeUs !== 0) {
      obj.annealTimeUs = Math.round(message.annealTimeUs);
    }
    return obj;
  },

  create(base?: DeepPartial<SetTarget>): SetTarget {
    return SetTarget.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetTarget>): SetTarget {
    const message = createBaseSetTarget();
    message.maxEnergyMilli = (object.maxEnergyMilli !== undefined && object.maxEnergyMilli !== null)
      ? BigInt(object.maxEnergyMilli)
      : 0n;
    message.minSolutions = object.minSolutions ?? 0;
    message.minDiversityMilli = object.minDiversityMilli ?? 0;
    message.numReads = object.numReads ?? 0;
    message.numSweeps = object.numSweeps ?? 0;
    message.annealTimeUs = object.annealTimeUs ?? 0;
    return message;
  },
};

function createBaseEdgeList(): EdgeList {
  return { u: [], v: [] };
}

export const EdgeList: MessageFns<EdgeList> = {
  encode(message: EdgeList, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.u) {
      writer.uint32(v);
    }
    writer.join();
    writer.uint32(18).fork();
    for (const v of message.v) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EdgeList {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseEdgeList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag === 8) {
            message.u.push(reader.uint32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.u.push(reader.uint32());
            }

            continue;
          }

          break;
        }
        case 2: {
          if (tag === 16) {
            message.v.push(reader.uint32());

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.v.push(reader.uint32());
            }

            continue;
          }

          break;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): EdgeList {
    return {
      u: globalThis.Array.isArray(object?.u) ? object.u.map((e: any) => globalThis.Number(e)) : [],
      v: globalThis.Array.isArray(object?.v) ? object.v.map((e: any) => globalThis.Number(e)) : [],
    };
  },

  toJSON(message: EdgeList): unknown {
    const obj: any = {};
    if (message.u?.length) {
      obj.u = message.u.map((e) => Math.round(e));
    }
    if (message.v?.length) {
      obj.v = message.v.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<EdgeList>): EdgeList {
    return EdgeList.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<EdgeList>): EdgeList {
    const message = createBaseEdgeList();
    message.u = object.u?.map((e) => e) || [];
    message.v = object.v?.map((e) => e) || [];
    return message;
  },
};

function createBaseIsingProblem(): IsingProblem {
  return {
    topologyHash: undefined,
    edges: undefined,
    hMilliLe32: new Uint8Array(0),
    jMilliLe32: new Uint8Array(0),
    numReads: 0,
    numSweeps: 0,
    annealTimeUs: 0,
    initialSpins: [],
    startBetaMilli: 0,
    reversalSMilli: 0,
    reversalPauseUs: 0,
  };
}

export const IsingProblem: MessageFns<IsingProblem> = {
  encode(message: IsingProblem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.topologyHash !== undefined) {
      writer.uint32(10).bytes(message.topologyHash);
    }
    if (message.edges !== undefined) {
      EdgeList.encode(message.edges, writer.uint32(18).fork()).join();
    }
    if (message.hMilliLe32.length !== 0) {
      writer.uint32(26).bytes(message.hMilliLe32);
    }
    if (message.jMilliLe32.length !== 0) {
      writer.uint32(34).bytes(message.jMilliLe32);
    }
    if (message.numReads !== 0) {
      writer.uint32(40).uint32(message.numReads);
    }
    if (message.numSweeps !== 0) {
      writer.uint32(56).uint32(message.numSweeps);
    }
    if (message.annealTimeUs !== 0) {
      writer.uint32(64).uint32(message.annealTimeUs);
    }
    for (const v of message.initialSpins) {
      writer.uint32(74).bytes(v!);
    }
    if (message.startBetaMilli !== 0) {
      writer.uint32(80).uint32(message.startBetaMilli);
    }
    if (message.reversalSMilli !== 0) {
      writer.uint32(88).uint32(message.reversalSMilli);
    }
    if (message.reversalPauseUs !== 0) {
      writer.uint32(96).uint32(message.reversalPauseUs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IsingProblem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIsingProblem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.topologyHash = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.edges = EdgeList.decode(reader, reader.uint32());
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.hMilliLe32 = reader.bytes();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.jMilliLe32 = reader.bytes();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.numReads = reader.uint32();
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.numSweeps = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.annealTimeUs = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.initialSpins.push(reader.bytes());
          continue;
        }
        case 10: {
          if (tag !== 80) {
            break;
          }

          message.startBetaMilli = reader.uint32();
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.reversalSMilli = reader.uint32();
          continue;
        }
        case 12: {
          if (tag !== 96) {
            break;
          }

          message.reversalPauseUs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IsingProblem {
    return {
      topologyHash: isSet(object.topologyHash)
        ? bytesFromBase64(object.topologyHash)
        : isSet(object.topology_hash)
        ? bytesFromBase64(object.topology_hash)
        : undefined,
      edges: isSet(object.edges) ? EdgeList.fromJSON(object.edges) : undefined,
      hMilliLe32: isSet(object.hMilliLe32)
        ? bytesFromBase64(object.hMilliLe32)
        : isSet(object.h_milli_le32)
        ? bytesFromBase64(object.h_milli_le32)
        : new Uint8Array(0),
      jMilliLe32: isSet(object.jMilliLe32)
        ? bytesFromBase64(object.jMilliLe32)
        : isSet(object.j_milli_le32)
        ? bytesFromBase64(object.j_milli_le32)
        : new Uint8Array(0),
      numReads: isSet(object.numReads)
        ? globalThis.Number(object.numReads)
        : isSet(object.num_reads)
        ? globalThis.Number(object.num_reads)
        : 0,
      numSweeps: isSet(object.numSweeps)
        ? globalThis.Number(object.numSweeps)
        : isSet(object.num_sweeps)
        ? globalThis.Number(object.num_sweeps)
        : 0,
      annealTimeUs: isSet(object.annealTimeUs)
        ? globalThis.Number(object.annealTimeUs)
        : isSet(object.anneal_time_us)
        ? globalThis.Number(object.anneal_time_us)
        : 0,
      initialSpins: globalThis.Array.isArray(object?.initialSpins)
        ? object.initialSpins.map((e: any) => bytesFromBase64(e))
        : globalThis.Array.isArray(object?.initial_spins)
        ? object.initial_spins.map((e: any) => bytesFromBase64(e))
        : [],
      startBetaMilli: isSet(object.startBetaMilli)
        ? globalThis.Number(object.startBetaMilli)
        : isSet(object.start_beta_milli)
        ? globalThis.Number(object.start_beta_milli)
        : 0,
      reversalSMilli: isSet(object.reversalSMilli)
        ? globalThis.Number(object.reversalSMilli)
        : isSet(object.reversal_s_milli)
        ? globalThis.Number(object.reversal_s_milli)
        : 0,
      reversalPauseUs: isSet(object.reversalPauseUs)
        ? globalThis.Number(object.reversalPauseUs)
        : isSet(object.reversal_pause_us)
        ? globalThis.Number(object.reversal_pause_us)
        : 0,
    };
  },

  toJSON(message: IsingProblem): unknown {
    const obj: any = {};
    if (message.topologyHash !== undefined) {
      obj.topologyHash = base64FromBytes(message.topologyHash);
    }
    if (message.edges !== undefined) {
      obj.edges = EdgeList.toJSON(message.edges);
    }
    if (message.hMilliLe32.length !== 0) {
      obj.hMilliLe32 = base64FromBytes(message.hMilliLe32);
    }
    if (message.jMilliLe32.length !== 0) {
      obj.jMilliLe32 = base64FromBytes(message.jMilliLe32);
    }
    if (message.numReads !== 0) {
      obj.numReads = Math.round(message.numReads);
    }
    if (message.numSweeps !== 0) {
      obj.numSweeps = Math.round(message.numSweeps);
    }
    if (message.annealTimeUs !== 0) {
      obj.annealTimeUs = Math.round(message.annealTimeUs);
    }
    if (message.initialSpins?.length) {
      obj.initialSpins = message.initialSpins.map((e) => base64FromBytes(e));
    }
    if (message.startBetaMilli !== 0) {
      obj.startBetaMilli = Math.round(message.startBetaMilli);
    }
    if (message.reversalSMilli !== 0) {
      obj.reversalSMilli = Math.round(message.reversalSMilli);
    }
    if (message.reversalPauseUs !== 0) {
      obj.reversalPauseUs = Math.round(message.reversalPauseUs);
    }
    return obj;
  },

  create(base?: DeepPartial<IsingProblem>): IsingProblem {
    return IsingProblem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IsingProblem>): IsingProblem {
    const message = createBaseIsingProblem();
    message.topologyHash = object.topologyHash ?? undefined;
    message.edges = (object.edges !== undefined && object.edges !== null)
      ? EdgeList.fromPartial(object.edges)
      : undefined;
    message.hMilliLe32 = object.hMilliLe32 ?? new Uint8Array(0);
    message.jMilliLe32 = object.jMilliLe32 ?? new Uint8Array(0);
    message.numReads = object.numReads ?? 0;
    message.numSweeps = object.numSweeps ?? 0;
    message.annealTimeUs = object.annealTimeUs ?? 0;
    message.initialSpins = object.initialSpins?.map((e) => e) || [];
    message.startBetaMilli = object.startBetaMilli ?? 0;
    message.reversalSMilli = object.reversalSMilli ?? 0;
    message.reversalPauseUs = object.reversalPauseUs ?? 0;
    return message;
  },
};

function createBaseProvenance(): Provenance {
  return { isPow: false, orderId: new Uint8Array(0) };
}

export const Provenance: MessageFns<Provenance> = {
  encode(message: Provenance, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.isPow !== false) {
      writer.uint32(8).bool(message.isPow);
    }
    if (message.orderId.length !== 0) {
      writer.uint32(18).bytes(message.orderId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Provenance {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProvenance();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.isPow = reader.bool();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.orderId = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Provenance {
    return {
      isPow: isSet(object.isPow)
        ? globalThis.Boolean(object.isPow)
        : isSet(object.is_pow)
        ? globalThis.Boolean(object.is_pow)
        : false,
      orderId: isSet(object.orderId)
        ? bytesFromBase64(object.orderId)
        : isSet(object.order_id)
        ? bytesFromBase64(object.order_id)
        : new Uint8Array(0),
    };
  },

  toJSON(message: Provenance): unknown {
    const obj: any = {};
    if (message.isPow !== false) {
      obj.isPow = message.isPow;
    }
    if (message.orderId.length !== 0) {
      obj.orderId = base64FromBytes(message.orderId);
    }
    return obj;
  },

  create(base?: DeepPartial<Provenance>): Provenance {
    return Provenance.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Provenance>): Provenance {
    const message = createBaseProvenance();
    message.isPow = object.isPow ?? false;
    message.orderId = object.orderId ?? new Uint8Array(0);
    return message;
  },
};

function createBaseJob(): Job {
  return { jobId: new Uint8Array(0), kind: 0, generation: 0n, deadlineMs: 0n, ising: undefined, provenance: undefined };
}

export const Job: MessageFns<Job> = {
  encode(message: Job, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId.length !== 0) {
      writer.uint32(10).bytes(message.jobId);
    }
    if (message.kind !== 0) {
      writer.uint32(16).int32(message.kind);
    }
    if (message.generation !== 0n) {
      if (BigInt.asUintN(64, message.generation) !== message.generation) {
        throw new globalThis.Error("value provided for field message.generation of type uint64 too large");
      }
      writer.uint32(24).uint64(message.generation);
    }
    if (message.deadlineMs !== 0n) {
      if (BigInt.asUintN(64, message.deadlineMs) !== message.deadlineMs) {
        throw new globalThis.Error("value provided for field message.deadlineMs of type uint64 too large");
      }
      writer.uint32(32).uint64(message.deadlineMs);
    }
    if (message.ising !== undefined) {
      IsingProblem.encode(message.ising, writer.uint32(42).fork()).join();
    }
    if (message.provenance !== undefined) {
      Provenance.encode(message.provenance, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Job {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJob();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.kind = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.generation = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.deadlineMs = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.ising = IsingProblem.decode(reader, reader.uint32());
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.provenance = Provenance.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Job {
    return {
      jobId: isSet(object.jobId)
        ? bytesFromBase64(object.jobId)
        : isSet(object.job_id)
        ? bytesFromBase64(object.job_id)
        : new Uint8Array(0),
      kind: isSet(object.kind) ? jobKindFromJSON(object.kind) : 0,
      generation: isSet(object.generation) ? BigInt(object.generation) : 0n,
      deadlineMs: isSet(object.deadlineMs)
        ? BigInt(object.deadlineMs)
        : isSet(object.deadline_ms)
        ? BigInt(object.deadline_ms)
        : 0n,
      ising: isSet(object.ising) ? IsingProblem.fromJSON(object.ising) : undefined,
      provenance: isSet(object.provenance) ? Provenance.fromJSON(object.provenance) : undefined,
    };
  },

  toJSON(message: Job): unknown {
    const obj: any = {};
    if (message.jobId.length !== 0) {
      obj.jobId = base64FromBytes(message.jobId);
    }
    if (message.kind !== 0) {
      obj.kind = jobKindToJSON(message.kind);
    }
    if (message.generation !== 0n) {
      obj.generation = message.generation.toString();
    }
    if (message.deadlineMs !== 0n) {
      obj.deadlineMs = message.deadlineMs.toString();
    }
    if (message.ising !== undefined) {
      obj.ising = IsingProblem.toJSON(message.ising);
    }
    if (message.provenance !== undefined) {
      obj.provenance = Provenance.toJSON(message.provenance);
    }
    return obj;
  },

  create(base?: DeepPartial<Job>): Job {
    return Job.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Job>): Job {
    const message = createBaseJob();
    message.jobId = object.jobId ?? new Uint8Array(0);
    message.kind = object.kind ?? 0;
    message.generation = (object.generation !== undefined && object.generation !== null)
      ? BigInt(object.generation)
      : 0n;
    message.deadlineMs = (object.deadlineMs !== undefined && object.deadlineMs !== null)
      ? BigInt(object.deadlineMs)
      : 0n;
    message.ising = (object.ising !== undefined && object.ising !== null)
      ? IsingProblem.fromPartial(object.ising)
      : undefined;
    message.provenance = (object.provenance !== undefined && object.provenance !== null)
      ? Provenance.fromPartial(object.provenance)
      : undefined;
    return message;
  },
};

function createBaseJobRequest(): JobRequest {
  return { credits: 0 };
}

export const JobRequest: MessageFns<JobRequest> = {
  encode(message: JobRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.credits !== 0) {
      writer.uint32(8).uint32(message.credits);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JobRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseJobRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.credits = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): JobRequest {
    return { credits: isSet(object.credits) ? globalThis.Number(object.credits) : 0 };
  },

  toJSON(message: JobRequest): unknown {
    const obj: any = {};
    if (message.credits !== 0) {
      obj.credits = Math.round(message.credits);
    }
    return obj;
  },

  create(base?: DeepPartial<JobRequest>): JobRequest {
    return JobRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<JobRequest>): JobRequest {
    const message = createBaseJobRequest();
    message.credits = object.credits ?? 0;
    return message;
  },
};

function createBaseCancel(): Cancel {
  return { maxGeneration: 0n };
}

export const Cancel: MessageFns<Cancel> = {
  encode(message: Cancel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.maxGeneration !== 0n) {
      if (BigInt.asUintN(64, message.maxGeneration) !== message.maxGeneration) {
        throw new globalThis.Error("value provided for field message.maxGeneration of type uint64 too large");
      }
      writer.uint32(8).uint64(message.maxGeneration);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Cancel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCancel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.maxGeneration = reader.uint64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Cancel {
    return {
      maxGeneration: isSet(object.maxGeneration)
        ? BigInt(object.maxGeneration)
        : isSet(object.max_generation)
        ? BigInt(object.max_generation)
        : 0n,
    };
  },

  toJSON(message: Cancel): unknown {
    const obj: any = {};
    if (message.maxGeneration !== 0n) {
      obj.maxGeneration = message.maxGeneration.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<Cancel>): Cancel {
    return Cancel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Cancel>): Cancel {
    const message = createBaseCancel();
    message.maxGeneration = (object.maxGeneration !== undefined && object.maxGeneration !== null)
      ? BigInt(object.maxGeneration)
      : 0n;
    return message;
  },
};

function createBasePing(): Ping {
  return {};
}

export const Ping: MessageFns<Ping> = {
  encode(_: Ping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Ping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePing();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): Ping {
    return {};
  },

  toJSON(_: Ping): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<Ping>): Ping {
    return Ping.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<Ping>): Ping {
    const message = createBasePing();
    return message;
  },
};

function createBaseShutdown(): Shutdown {
  return { graceMs: 0 };
}

export const Shutdown: MessageFns<Shutdown> = {
  encode(message: Shutdown, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.graceMs !== 0) {
      writer.uint32(8).uint32(message.graceMs);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Shutdown {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShutdown();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.graceMs = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Shutdown {
    return {
      graceMs: isSet(object.graceMs)
        ? globalThis.Number(object.graceMs)
        : isSet(object.grace_ms)
        ? globalThis.Number(object.grace_ms)
        : 0,
    };
  },

  toJSON(message: Shutdown): unknown {
    const obj: any = {};
    if (message.graceMs !== 0) {
      obj.graceMs = Math.round(message.graceMs);
    }
    return obj;
  },

  create(base?: DeepPartial<Shutdown>): Shutdown {
    return Shutdown.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Shutdown>): Shutdown {
    const message = createBaseShutdown();
    message.graceMs = object.graceMs ?? 0;
    return message;
  },
};

function createBaseSolution(): Solution {
  return { spinsBytes: new Uint8Array(0), energyMilli: 0n };
}

export const Solution: MessageFns<Solution> = {
  encode(message: Solution, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.spinsBytes.length !== 0) {
      writer.uint32(10).bytes(message.spinsBytes);
    }
    if (message.energyMilli !== 0n) {
      if (BigInt.asIntN(64, message.energyMilli) !== message.energyMilli) {
        throw new globalThis.Error("value provided for field message.energyMilli of type int64 too large");
      }
      writer.uint32(16).int64(message.energyMilli);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Solution {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSolution();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.spinsBytes = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.energyMilli = reader.int64() as bigint;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Solution {
    return {
      spinsBytes: isSet(object.spinsBytes)
        ? bytesFromBase64(object.spinsBytes)
        : isSet(object.spins_bytes)
        ? bytesFromBase64(object.spins_bytes)
        : new Uint8Array(0),
      energyMilli: isSet(object.energyMilli)
        ? BigInt(object.energyMilli)
        : isSet(object.energy_milli)
        ? BigInt(object.energy_milli)
        : 0n,
    };
  },

  toJSON(message: Solution): unknown {
    const obj: any = {};
    if (message.spinsBytes.length !== 0) {
      obj.spinsBytes = base64FromBytes(message.spinsBytes);
    }
    if (message.energyMilli !== 0n) {
      obj.energyMilli = message.energyMilli.toString();
    }
    return obj;
  },

  create(base?: DeepPartial<Solution>): Solution {
    return Solution.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Solution>): Solution {
    const message = createBaseSolution();
    message.spinsBytes = object.spinsBytes ?? new Uint8Array(0);
    message.energyMilli = (object.energyMilli !== undefined && object.energyMilli !== null)
      ? BigInt(object.energyMilli)
      : 0n;
    return message;
  },
};

function createBaseSamplerMeta(): SamplerMeta {
  return { reads: 0, sweeps: 0, deviceAccessTimeUs: 0n, qpuAccessUs: 0n, extra: {} };
}

export const SamplerMeta: MessageFns<SamplerMeta> = {
  encode(message: SamplerMeta, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reads !== 0) {
      writer.uint32(8).uint32(message.reads);
    }
    if (message.sweeps !== 0) {
      writer.uint32(16).uint32(message.sweeps);
    }
    if (message.deviceAccessTimeUs !== 0n) {
      if (BigInt.asUintN(64, message.deviceAccessTimeUs) !== message.deviceAccessTimeUs) {
        throw new globalThis.Error("value provided for field message.deviceAccessTimeUs of type uint64 too large");
      }
      writer.uint32(24).uint64(message.deviceAccessTimeUs);
    }
    if (message.qpuAccessUs !== 0n) {
      if (BigInt.asUintN(64, message.qpuAccessUs) !== message.qpuAccessUs) {
        throw new globalThis.Error("value provided for field message.qpuAccessUs of type uint64 too large");
      }
      writer.uint32(32).uint64(message.qpuAccessUs);
    }
    globalThis.Object.entries(message.extra).forEach(([key, value]: [string, string]) => {
      SamplerMeta_ExtraEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SamplerMeta {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSamplerMeta();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.reads = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sweeps = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.deviceAccessTimeUs = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.qpuAccessUs = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = SamplerMeta_ExtraEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.extra[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SamplerMeta {
    return {
      reads: isSet(object.reads) ? globalThis.Number(object.reads) : 0,
      sweeps: isSet(object.sweeps) ? globalThis.Number(object.sweeps) : 0,
      deviceAccessTimeUs: isSet(object.deviceAccessTimeUs)
        ? BigInt(object.deviceAccessTimeUs)
        : isSet(object.device_access_time_us)
        ? BigInt(object.device_access_time_us)
        : 0n,
      qpuAccessUs: isSet(object.qpuAccessUs)
        ? BigInt(object.qpuAccessUs)
        : isSet(object.qpu_access_us)
        ? BigInt(object.qpu_access_us)
        : 0n,
      extra: isObject(object.extra)
        ? (globalThis.Object.entries(object.extra) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: SamplerMeta): unknown {
    const obj: any = {};
    if (message.reads !== 0) {
      obj.reads = Math.round(message.reads);
    }
    if (message.sweeps !== 0) {
      obj.sweeps = Math.round(message.sweeps);
    }
    if (message.deviceAccessTimeUs !== 0n) {
      obj.deviceAccessTimeUs = message.deviceAccessTimeUs.toString();
    }
    if (message.qpuAccessUs !== 0n) {
      obj.qpuAccessUs = message.qpuAccessUs.toString();
    }
    if (message.extra) {
      const entries = globalThis.Object.entries(message.extra) as [string, string][];
      if (entries.length > 0) {
        obj.extra = {};
        entries.forEach(([k, v]) => {
          obj.extra[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<SamplerMeta>): SamplerMeta {
    return SamplerMeta.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SamplerMeta>): SamplerMeta {
    const message = createBaseSamplerMeta();
    message.reads = object.reads ?? 0;
    message.sweeps = object.sweeps ?? 0;
    message.deviceAccessTimeUs = (object.deviceAccessTimeUs !== undefined && object.deviceAccessTimeUs !== null)
      ? BigInt(object.deviceAccessTimeUs)
      : 0n;
    message.qpuAccessUs = (object.qpuAccessUs !== undefined && object.qpuAccessUs !== null)
      ? BigInt(object.qpuAccessUs)
      : 0n;
    message.extra = (globalThis.Object.entries(object.extra ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseSamplerMeta_ExtraEntry(): SamplerMeta_ExtraEntry {
  return { key: "", value: "" };
}

export const SamplerMeta_ExtraEntry: MessageFns<SamplerMeta_ExtraEntry> = {
  encode(message: SamplerMeta_ExtraEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SamplerMeta_ExtraEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSamplerMeta_ExtraEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SamplerMeta_ExtraEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: SamplerMeta_ExtraEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<SamplerMeta_ExtraEntry>): SamplerMeta_ExtraEntry {
    return SamplerMeta_ExtraEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SamplerMeta_ExtraEntry>): SamplerMeta_ExtraEntry {
    const message = createBaseSamplerMeta_ExtraEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseResult(): Result {
  return { jobId: new Uint8Array(0), solutions: [], meta: undefined };
}

export const Result: MessageFns<Result> = {
  encode(message: Result, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId.length !== 0) {
      writer.uint32(10).bytes(message.jobId);
    }
    for (const v of message.solutions) {
      Solution.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.meta !== undefined) {
      SamplerMeta.encode(message.meta, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Result {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseResult();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.solutions.push(Solution.decode(reader, reader.uint32()));
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.meta = SamplerMeta.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Result {
    return {
      jobId: isSet(object.jobId)
        ? bytesFromBase64(object.jobId)
        : isSet(object.job_id)
        ? bytesFromBase64(object.job_id)
        : new Uint8Array(0),
      solutions: globalThis.Array.isArray(object?.solutions)
        ? object.solutions.map((e: any) => Solution.fromJSON(e))
        : [],
      meta: isSet(object.meta) ? SamplerMeta.fromJSON(object.meta) : undefined,
    };
  },

  toJSON(message: Result): unknown {
    const obj: any = {};
    if (message.jobId.length !== 0) {
      obj.jobId = base64FromBytes(message.jobId);
    }
    if (message.solutions?.length) {
      obj.solutions = message.solutions.map((e) => Solution.toJSON(e));
    }
    if (message.meta !== undefined) {
      obj.meta = SamplerMeta.toJSON(message.meta);
    }
    return obj;
  },

  create(base?: DeepPartial<Result>): Result {
    return Result.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Result>): Result {
    const message = createBaseResult();
    message.jobId = object.jobId ?? new Uint8Array(0);
    message.solutions = object.solutions?.map((e) => Solution.fromPartial(e)) || [];
    message.meta = (object.meta !== undefined && object.meta !== null)
      ? SamplerMeta.fromPartial(object.meta)
      : undefined;
    return message;
  },
};

function createBaseReject(): Reject {
  return { jobId: new Uint8Array(0), reason: 0 };
}

export const Reject: MessageFns<Reject> = {
  encode(message: Reject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.jobId.length !== 0) {
      writer.uint32(10).bytes(message.jobId);
    }
    if (message.reason !== 0) {
      writer.uint32(16).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.jobId = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Reject {
    return {
      jobId: isSet(object.jobId)
        ? bytesFromBase64(object.jobId)
        : isSet(object.job_id)
        ? bytesFromBase64(object.job_id)
        : new Uint8Array(0),
      reason: isSet(object.reason) ? rejectReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: Reject): unknown {
    const obj: any = {};
    if (message.jobId.length !== 0) {
      obj.jobId = base64FromBytes(message.jobId);
    }
    if (message.reason !== 0) {
      obj.reason = rejectReasonToJSON(message.reason);
    }
    return obj;
  },

  create(base?: DeepPartial<Reject>): Reject {
    return Reject.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Reject>): Reject {
    const message = createBaseReject();
    message.jobId = object.jobId ?? new Uint8Array(0);
    message.reason = object.reason ?? 0;
    return message;
  },
};

function createBaseStatus(): Status {
  return { minerId: "", utilization: 0, jobsDone: 0n, abandonedGeneration: 0n, samplerStats: {} };
}

export const Status: MessageFns<Status> = {
  encode(message: Status, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.minerId !== "") {
      writer.uint32(10).string(message.minerId);
    }
    if (message.utilization !== 0) {
      writer.uint32(17).double(message.utilization);
    }
    if (message.jobsDone !== 0n) {
      if (BigInt.asUintN(64, message.jobsDone) !== message.jobsDone) {
        throw new globalThis.Error("value provided for field message.jobsDone of type uint64 too large");
      }
      writer.uint32(24).uint64(message.jobsDone);
    }
    if (message.abandonedGeneration !== 0n) {
      if (BigInt.asUintN(64, message.abandonedGeneration) !== message.abandonedGeneration) {
        throw new globalThis.Error("value provided for field message.abandonedGeneration of type uint64 too large");
      }
      writer.uint32(32).uint64(message.abandonedGeneration);
    }
    globalThis.Object.entries(message.samplerStats).forEach(([key, value]: [string, string]) => {
      Status_SamplerStatsEntry.encode({ key: key as any, value }, writer.uint32(42).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Status {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatus();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.minerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.utilization = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.jobsDone = reader.uint64() as bigint;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.abandonedGeneration = reader.uint64() as bigint;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          const entry5 = Status_SamplerStatsEntry.decode(reader, reader.uint32());
          if (entry5.value !== undefined) {
            message.samplerStats[entry5.key] = entry5.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Status {
    return {
      minerId: isSet(object.minerId)
        ? globalThis.String(object.minerId)
        : isSet(object.miner_id)
        ? globalThis.String(object.miner_id)
        : "",
      utilization: isSet(object.utilization) ? globalThis.Number(object.utilization) : 0,
      jobsDone: isSet(object.jobsDone)
        ? BigInt(object.jobsDone)
        : isSet(object.jobs_done)
        ? BigInt(object.jobs_done)
        : 0n,
      abandonedGeneration: isSet(object.abandonedGeneration)
        ? BigInt(object.abandonedGeneration)
        : isSet(object.abandoned_generation)
        ? BigInt(object.abandoned_generation)
        : 0n,
      samplerStats: isObject(object.samplerStats)
        ? (globalThis.Object.entries(object.samplerStats) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : isObject(object.sampler_stats)
        ? (globalThis.Object.entries(object.sampler_stats) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: Status): unknown {
    const obj: any = {};
    if (message.minerId !== "") {
      obj.minerId = message.minerId;
    }
    if (message.utilization !== 0) {
      obj.utilization = message.utilization;
    }
    if (message.jobsDone !== 0n) {
      obj.jobsDone = message.jobsDone.toString();
    }
    if (message.abandonedGeneration !== 0n) {
      obj.abandonedGeneration = message.abandonedGeneration.toString();
    }
    if (message.samplerStats) {
      const entries = globalThis.Object.entries(message.samplerStats) as [string, string][];
      if (entries.length > 0) {
        obj.samplerStats = {};
        entries.forEach(([k, v]) => {
          obj.samplerStats[k] = v;
        });
      }
    }
    return obj;
  },

  create(base?: DeepPartial<Status>): Status {
    return Status.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Status>): Status {
    const message = createBaseStatus();
    message.minerId = object.minerId ?? "";
    message.utilization = object.utilization ?? 0;
    message.jobsDone = (object.jobsDone !== undefined && object.jobsDone !== null) ? BigInt(object.jobsDone) : 0n;
    message.abandonedGeneration = (object.abandonedGeneration !== undefined && object.abandonedGeneration !== null)
      ? BigInt(object.abandonedGeneration)
      : 0n;
    message.samplerStats = (globalThis.Object.entries(object.samplerStats ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseStatus_SamplerStatsEntry(): Status_SamplerStatsEntry {
  return { key: "", value: "" };
}

export const Status_SamplerStatsEntry: MessageFns<Status_SamplerStatsEntry> = {
  encode(message: Status_SamplerStatsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Status_SamplerStatsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStatus_SamplerStatsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Status_SamplerStatsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: Status_SamplerStatsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<Status_SamplerStatsEntry>): Status_SamplerStatsEntry {
    return Status_SamplerStatsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Status_SamplerStatsEntry>): Status_SamplerStatsEntry {
    const message = createBaseStatus_SamplerStatsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseFatal(): Fatal {
  return { exitCode: 0, reason: "", restartRequired: false };
}

export const Fatal: MessageFns<Fatal> = {
  encode(message: Fatal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.exitCode !== 0) {
      writer.uint32(8).uint32(message.exitCode);
    }
    if (message.reason !== "") {
      writer.uint32(18).string(message.reason);
    }
    if (message.restartRequired !== false) {
      writer.uint32(24).bool(message.restartRequired);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fatal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFatal();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.exitCode = reader.uint32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.reason = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.restartRequired = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fatal {
    return {
      exitCode: isSet(object.exitCode)
        ? globalThis.Number(object.exitCode)
        : isSet(object.exit_code)
        ? globalThis.Number(object.exit_code)
        : 0,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
      restartRequired: isSet(object.restartRequired)
        ? globalThis.Boolean(object.restartRequired)
        : isSet(object.restart_required)
        ? globalThis.Boolean(object.restart_required)
        : false,
    };
  },

  toJSON(message: Fatal): unknown {
    const obj: any = {};
    if (message.exitCode !== 0) {
      obj.exitCode = Math.round(message.exitCode);
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    if (message.restartRequired !== false) {
      obj.restartRequired = message.restartRequired;
    }
    return obj;
  },

  create(base?: DeepPartial<Fatal>): Fatal {
    return Fatal.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fatal>): Fatal {
    const message = createBaseFatal();
    message.exitCode = object.exitCode ?? 0;
    message.reason = object.reason ?? "";
    message.restartRequired = object.restartRequired ?? false;
    return message;
  },
};

function createBaseGetCapabilities(): GetCapabilities {
  return {};
}

export const GetCapabilities: MessageFns<GetCapabilities> = {
  encode(_: GetCapabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetCapabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): GetCapabilities {
    return {};
  },

  toJSON(_: GetCapabilities): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<GetCapabilities>): GetCapabilities {
    return GetCapabilities.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<GetCapabilities>): GetCapabilities {
    const message = createBaseGetCapabilities();
    return message;
  },
};

function createBaseCapabilities(): Capabilities {
  return {
    backend: "",
    algorithm: "",
    supportedKinds: [],
    maxNodes: 0,
    maxEdges: 0,
    features: [],
    protocolVersion: 0,
    streamWidth: 0,
    nativeTopologyHash: undefined,
  };
}

export const Capabilities: MessageFns<Capabilities> = {
  encode(message: Capabilities, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.backend !== "") {
      writer.uint32(10).string(message.backend);
    }
    if (message.algorithm !== "") {
      writer.uint32(18).string(message.algorithm);
    }
    writer.uint32(26).fork();
    for (const v of message.supportedKinds) {
      writer.int32(v);
    }
    writer.join();
    if (message.maxNodes !== 0) {
      writer.uint32(32).uint32(message.maxNodes);
    }
    if (message.maxEdges !== 0) {
      writer.uint32(40).uint32(message.maxEdges);
    }
    for (const v of message.features) {
      writer.uint32(50).string(v!);
    }
    if (message.protocolVersion !== 0) {
      writer.uint32(56).uint32(message.protocolVersion);
    }
    if (message.streamWidth !== 0) {
      writer.uint32(64).uint32(message.streamWidth);
    }
    if (message.nativeTopologyHash !== undefined) {
      writer.uint32(74).bytes(message.nativeTopologyHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capabilities {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapabilities();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.backend = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.algorithm = reader.string();
          continue;
        }
        case 3: {
          if (tag === 24) {
            message.supportedKinds.push(reader.int32() as any);

            continue;
          }

          if (tag === 26) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.supportedKinds.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.maxNodes = reader.uint32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxEdges = reader.uint32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.features.push(reader.string());
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.protocolVersion = reader.uint32();
          continue;
        }
        case 8: {
          if (tag !== 64) {
            break;
          }

          message.streamWidth = reader.uint32();
          continue;
        }
        case 9: {
          if (tag !== 74) {
            break;
          }

          message.nativeTopologyHash = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capabilities {
    return {
      backend: isSet(object.backend) ? globalThis.String(object.backend) : "",
      algorithm: isSet(object.algorithm) ? globalThis.String(object.algorithm) : "",
      supportedKinds: globalThis.Array.isArray(object?.supportedKinds)
        ? object.supportedKinds.map((e: any) => jobKindFromJSON(e))
        : globalThis.Array.isArray(object?.supported_kinds)
        ? object.supported_kinds.map((e: any) => jobKindFromJSON(e))
        : [],
      maxNodes: isSet(object.maxNodes)
        ? globalThis.Number(object.maxNodes)
        : isSet(object.max_nodes)
        ? globalThis.Number(object.max_nodes)
        : 0,
      maxEdges: isSet(object.maxEdges)
        ? globalThis.Number(object.maxEdges)
        : isSet(object.max_edges)
        ? globalThis.Number(object.max_edges)
        : 0,
      features: globalThis.Array.isArray(object?.features) ? object.features.map((e: any) => globalThis.String(e)) : [],
      protocolVersion: isSet(object.protocolVersion)
        ? globalThis.Number(object.protocolVersion)
        : isSet(object.protocol_version)
        ? globalThis.Number(object.protocol_version)
        : 0,
      streamWidth: isSet(object.streamWidth)
        ? globalThis.Number(object.streamWidth)
        : isSet(object.stream_width)
        ? globalThis.Number(object.stream_width)
        : 0,
      nativeTopologyHash: isSet(object.nativeTopologyHash)
        ? bytesFromBase64(object.nativeTopologyHash)
        : isSet(object.native_topology_hash)
        ? bytesFromBase64(object.native_topology_hash)
        : undefined,
    };
  },

  toJSON(message: Capabilities): unknown {
    const obj: any = {};
    if (message.backend !== "") {
      obj.backend = message.backend;
    }
    if (message.algorithm !== "") {
      obj.algorithm = message.algorithm;
    }
    if (message.supportedKinds?.length) {
      obj.supportedKinds = message.supportedKinds.map((e) => jobKindToJSON(e));
    }
    if (message.maxNodes !== 0) {
      obj.maxNodes = Math.round(message.maxNodes);
    }
    if (message.maxEdges !== 0) {
      obj.maxEdges = Math.round(message.maxEdges);
    }
    if (message.features?.length) {
      obj.features = message.features;
    }
    if (message.protocolVersion !== 0) {
      obj.protocolVersion = Math.round(message.protocolVersion);
    }
    if (message.streamWidth !== 0) {
      obj.streamWidth = Math.round(message.streamWidth);
    }
    if (message.nativeTopologyHash !== undefined) {
      obj.nativeTopologyHash = base64FromBytes(message.nativeTopologyHash);
    }
    return obj;
  },

  create(base?: DeepPartial<Capabilities>): Capabilities {
    return Capabilities.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Capabilities>): Capabilities {
    const message = createBaseCapabilities();
    message.backend = object.backend ?? "";
    message.algorithm = object.algorithm ?? "";
    message.supportedKinds = object.supportedKinds?.map((e) => e) || [];
    message.maxNodes = object.maxNodes ?? 0;
    message.maxEdges = object.maxEdges ?? 0;
    message.features = object.features?.map((e) => e) || [];
    message.protocolVersion = object.protocolVersion ?? 0;
    message.streamWidth = object.streamWidth ?? 0;
    message.nativeTopologyHash = object.nativeTopologyHash ?? undefined;
    return message;
  },
};

export type MinerServiceService = typeof MinerServiceService;
export const MinerServiceService = {
  session: {
    path: "/quip.v1.MinerService/Session" as const,
    requestStream: true as const,
    responseStream: true as const,
    requestSerialize: (value: MinerMsg): Buffer => Buffer.from(MinerMsg.encode(value).finish()),
    requestDeserialize: (value: Buffer): MinerMsg => MinerMsg.decode(value),
    responseSerialize: (value: CoordMsg): Buffer => Buffer.from(CoordMsg.encode(value).finish()),
    responseDeserialize: (value: Buffer): CoordMsg => CoordMsg.decode(value),
  },
} as const;

export interface MinerServiceServer extends UntypedServiceImplementation {
  session: handleBidiStreamingCall<MinerMsg, CoordMsg>;
}

export interface MinerServiceClient extends Client {
  session(): ClientDuplexStream<MinerMsg, CoordMsg>;
  session(options: Partial<CallOptions>): ClientDuplexStream<MinerMsg, CoordMsg>;
  session(metadata: Metadata, options?: Partial<CallOptions>): ClientDuplexStream<MinerMsg, CoordMsg>;
}

export const MinerServiceClient = makeGenericClientConstructor(
  MinerServiceService,
  "quip.v1.MinerService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): MinerServiceClient;
  service: typeof MinerServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from((globalThis as any).Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return (globalThis as any).Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
