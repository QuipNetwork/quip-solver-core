[package]
name = "quip-proto"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
tonic = { workspace = true }
prost = { workspace = true }

[build-dependencies]
tonic-build = { workspace = true }

# quip-proto is 100% prost/tonic-generated (tonic::include_proto!); it stays on
# default lints rather than opting into the strict workspace set.
